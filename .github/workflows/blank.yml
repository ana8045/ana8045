name: multiplejobs
on: 
   push:
      branches:
               [ main ]
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
    - name: build
      run: echo " Build "
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test
      run: echo " TEST "
      
