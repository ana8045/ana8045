apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: app-EKS
spec:
  selector: 
    app: myapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP 

 